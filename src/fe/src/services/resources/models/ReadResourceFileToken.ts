export interface ReadResourceFileTokenQuery {
  Id: string;
}

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface ReadResourceFileTokenResponse {
  FileToken: string;
  FileCompleteName: string;
}
