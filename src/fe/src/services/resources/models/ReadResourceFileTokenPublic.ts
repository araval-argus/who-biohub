export interface ReadResourceFileTokenPublicQuery {
  Id: string;
}

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface ReadResourceFileTokenPublicResponse {
  FileToken: string;
  FileCompleteName: string;
}
