export interface CheckCurrentsForDeleteQuery {
  Id: string;
}

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface CheckCurrentsForDeleteResponse {
  FolderContainsCurrent: boolean;
}
